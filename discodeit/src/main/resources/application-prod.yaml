spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

discodeit:
  storage:
    type: ${DISCODEIT_STORAGE_TYPE}

server:
  port: ${PORT:8080}


