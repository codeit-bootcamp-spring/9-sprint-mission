spring:
  application:
    name: "discodeit"

server:
  port: ${PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html  # 접속 경로 설정
    groups-order: DESC
  api-docs:
    path: /api-docs

discodeit:
  repository:
    type: file        # jcf | file
    file-directory: .discodeit